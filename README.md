# HibernateWithSpringBootCRUD
This repository is an example of Hibernate with Spring Boot with CRUD Rest API
Change the application.properties and put the configuration of the local db / Server db as needed
